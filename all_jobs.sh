#! /bin/bash

source $VARIABLES

mkdir -p $LOG_DIR

SOFTWARE_PATH=$SOFTWARE_PATH $EXPERIMENT_DIR/init.sh 2>&1 > $LOG_DIR/init.log

VARIABLES=$VARIABLES xargs -a $BENCHMARK_PATH/$BENCHMARK_LIST -L $BENCHS_PER_JOB ./schedule_job.sh