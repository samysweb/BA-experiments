#! /bin/bash

source $VARIABLES

LOG_DIR=$LOG_DIR_BASE`date -Iminutes`"/"

mkdir -p $LOG_DIR

EXPERIMENT_DIR=$EXPERIMENT_DIR SOFTWARE_PATH=$SOFTWARE_PATH $EXPERIMENT_DIR/init.sh 2>&1 > $LOG_DIR/init.log

VARIABLES=$VARIABLES LOG_DIR=$LOG_DIR xargs -a $BENCHMARK_PATH/$BENCHMARK_LIST -L $BENCHS_PER_JOB ./schedule_job.sh